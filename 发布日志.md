# ECFlow 发布日志

---

## v2

在 v1 的基础上系统重构与整合，更名为 **ECFlow**。相对 v1 的主要增量：

- **问题模块重构**：v1 的问题定义为单体结构；v2 将其**横向拆为变量 / 目标 / 约束 / 启发四域**，每域"定义 → 编译 → 运行期引擎"纵向分层，优化器只通过问题句柄与之交互。定义与求解分离、四域可独立演进。
- **约束体系扩展**：在 v1 约束能力之上补齐为**域 / 基数 / 累积 / 图 / 表达式**多族，其中表达式约束支持**区间传播**（域缩减）。
- **问题模板扩充**：模板数由 v1 的 4 类扩充到 **14 类**（新增 QAP、CFLP、CPMP、CVRP、FJSP、MDS、MIS、SR、STP、WFS 等，并统一各家族的文件载入格式）。
- **算法覆盖扩展**：在 v1 的 PSO/ACO/DE/EDA/ES/GA 之上，批量补充**近十年元启发算法**（CMA-ES、灰狼 / 鲸鱼等系列、免疫、烟花、局部搜索…）。
- **算子原语化 · 流式编排**：把 v1 中较整体化的算法算子**拆解为更细的原语**，使一代内的"生成 → 评估 → 选择"能像流水线一样**分段自由编排**（如 GA 的交叉段 + 变异段、免疫的双段选择），不再受限于固定的算法框架——新算法即原语的新组合。
- **组件架构现代化**：由 v1 的工厂 / 类型 / 设值器体系改为**自注册表**——加一个算子 = 一个文件 + 一行注册，装配层零改动；装配期做**组件间契约校验**。
- **个体模型**：个体改为**特性袋组合**（velocity/pbest/sigma 等由装配期推断挂载），免去逐算法的个体子类。
- **配置构筑优化**：实现了`ConfigBuilder` fluent 装配，同时优化配置表的解编码形式，提高配置表的可读性。
- **可执行程序**：新增命令行工具 `ecflow`，问题 / 配置从文件载入并运行，完整参数（种子 / 次数 / 输出 / 预算覆盖 / 静默 / 进度）。
- **跨平台构建**：新增 CMake 与 Visual Studio 两套工程，MSVC 静态运行时、产物自包含。

## v1

一个基于演化计算（EC）的**模块化 C++ 工具库**，面向组合优化，提供便捷的问题定义接口，支持**组合约束**与**启发信息**的定义。核心能力：

- **问题建模**：问题定义与运行期句柄、约束（含可行域 / 区间）、启发信息（inspiration）、公式求值（calculator）。
- **问题模板**：4 类开箱模板——TSP、MKP、QAP，以及数值基准（numerical）。
- **优化器框架**：一个**模块化、通用型的演化框架**——可插拔的学习拓扑（决定谁向谁学）、学习策略（决定怎么生成子代）、环境选择、初始化、最优档案等算子模块，装配到多子群（swarm / subswarm，含子群管理 / 拓扑 / 邻域）结构上，配终止器与日志驱动。
- **优化算法**：基于上述框架实现的 PSO（含 BPSO / SetPSO）、ACO、DE、EDA、ES、GA等策略。
- **配置与校验**：配置装配（builder + configure-list）+ 装配期断言匹配（assert-matcher），并支持配置的**文件 save/load**（`.cfg`，文本 / JSON），配置与代码解耦、可存盘复用。
- **结果分析**：ec-analyzer 指标分析，多格式输出（txt / csv / latex）。
