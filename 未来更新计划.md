# ECFlow 未来更新计划

下一大版本聚焦**让框架更易用、可视、可交互、更智能，并提升多子群配置的运行效率**。重点开发项：

## 1. 可视化 UI

图形界面，覆盖"配问题 → 配优化器 → 跑 → 看结果"全流程，让不写代码的用户也能使用。核心是**拖拽式积木拼接的配置构建器**：每个算子是一块带类型化接口与参数槽的积木，拖入流水线即拼成一条优化配置，卡扣合法性实时校验。另含跑动中的**实时监视器**（收敛曲线 / 种群分布）、**解的可视化**（如 TSP 巡回图、调度甘特图）与**多算法结果对比看板**。

## 2. 交互式 CLI

面向人工探索与调试的交互式命令行：交互向导一问一答建问题 / 配置（免手写配置文件）、子命令（列问题 / 配置 / 组件、跑、分析）、会话态（加载一次连续跑多种子 / 多算法对比）、联机帮助。

## 3. 子种群多线程并行

在多子种群（island-model）配置下，把每一轮各子群的独立演化**并行到多线程**以缩短运行时间。需要重新设计并行下的预算记账与线程安全的最优档案。

## 4. 基于大模型的智能算法生成与推荐

用大语言模型（LLM）为优化任务**自动生成配置**、并按问题**推荐算法**，把"选算法 / 调参"从人工经验转为智能辅助。这与框架的机读设计天然契合：组件注册表提供可选算子清单、参数模板提供每个参数的类型 / 范围 / 推荐区间、装配期契约给出组件间兼容规则——LLM 据此可生成**合法且成体系**的优化配置。

- **算法生成**：给定问题描述，LLM 组合算子流水线、填参数，产出可直接运行的配置（合法性由装配期校验兜底）。
- **算法推荐**：从问题特征（编码类型 / 规模 / 约束结构 / 是否连续…）出发，推荐适配的算法族与配置，并可结合历史运行结果迭代改进。
- **自然语言接口**：用户用一句话描述需求，系统给出配置与解释。可作为可视化 UI / 交互式 CLI 之上的智能层。

---

其余规划（新算法算子、更强约束传播、评估分析扩展等）随版本迭代推进。
